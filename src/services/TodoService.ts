import { Todo } from '../interfaces/todo.interface';
export class TodoService {
    private API_URL: string  = import.meta.env.VITE_API_URL;

    getTodos(_id: string | null){

    }

    createTodo(){
        
    }
}